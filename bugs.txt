1. Entry=exit -> fixed
1b. Keys after esc
2. Entry/exit on 42
3. Show_path validation
3. Theme validation -> fixed
5. Entry/exit validation
6. Recursive dfs in big map

hi bro those are bugs i could find until now they are big and can be a reason we faild and lose 3 eval points 

1- entry=exit after playing with "wsad" enty keep changing and we entry = exit and we exit it save that which is refuesd by subject tottaly so if they run the verfier it will be wrong
to fix it is easy with do a copy_maz that one that we will play with but orignal one is what we going to save

small erro in same context if i type esc next 2 moves or key are not working then later will be working again cause afte esc it excepcet 2 character  


2- if enrty or exit in 42 pattern then we have problem and since there is no valid path cause cell is closed so we have no maze
for this one i dont know to be honset solution we change posittion of 42 or simply refuse entry/exit there 

3-for show_path or animated they are bad parrsed cause you call right function but you didnt use the error from there you used expect which is not workking cause there is nevere exception there already handelled so if error then append  otherwise the code will fail cause None later will be used 
its easy to fix just verfier erro from pars_bool if not None then append it 

4- for THEME you made function called _parse_theme but you never used so that make any string accepeted so if i use "blabla" is accepted and that will create error cause dictionry dosn't have that 

5- for entry exit again our code will crush cause we removed the check i put first time after parrseing each attrubit we dont check and return errors directly so if entry is bad we continy and then we check point[0] which is litteraly None[0] 
solution we just put first version i did check errors after parsing then returning them if any fail in parsing before move next step

